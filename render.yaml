services:
  - type: web
    name: autonomous-code-wizard-api
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm run server-prod
    envVars:
      - key: NODE_ENV
        value: production